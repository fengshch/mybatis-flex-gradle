
dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("./libs.versions.toml"))
        }
    }
}

rootProject.name = 'mybatis-flex-gradle'
includeBuild('mybatis-flex-gradle-plugin')
include 'example'

